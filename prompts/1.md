Create a Spring Boot 4.0.5 microservice called "order-service" with the following specifications:

- Group ID: org.example
- Artifact ID: order-service
- Layered package structure under org.example.orderservice:
    - controller
    - service
    - repository
    - model
    - dto
    - mapper
    - config
    - exception
    - event

Dependencies to include in pom.xml:
- spring-boot-starter-webmvc
- spring-boot-starter-data-jpa
- spring-boot-starter-validation
- spring-boot-starter-actuator
- spring-boot-starter-security
- springdoc-openapi-starter-webmvc-ui
- liquibase-core
- postgresql driver
- lombok
- mapstruct and mapstruct-processor
- spring-boot-starter-test
- testcontainers-junit-jupiter and testcontainers-postgresql
- spring-retry and spring-boot-starter-aspectj
- micrometer-registry-prometheus
- net.logstash.logback:logstash-logback-encoder for structured logging
- com.bucket4j:bucket4j-core:8.10.1 for per-IP rate limiting on the auth endpoint

Create application.yml with profiles. PostgreSQL is used on ALL profiles — no H2.
- default/dev: PostgreSQL with env-var overrides and sensible local defaults:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:orderdb}
    username: ${DB_USER:orderuser} / password: ${DB_PASSWORD:orderpass}
    HikariCP: maximum-pool-size=5, minimum-idle=1
    logging level DEBUG, show-sql: true
- staging: PostgreSQL via env vars (DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD), connection pool of 10
- prod: same as staging but connection pool of 30, logging level WARN
- Common (all profiles):
  - server.port: 8080
  - spring.application.name: order-service
  - spring.jpa.open-in-view: false
  - spring.jackson.default-property-inclusion: non_null
  - spring.liquibase.change-log: classpath:db/changelog/db.changelog-master.yaml

Include:
- A .gitignore covering: target/, .idea/, *.iml, .DS_Store, *.env
- Include Maven wrapper (mvnw/mvnw.cmd) — or specify which Maven version the Dockerfile should use
- In maven-compiler-plugin, configure annotation processor paths with Lombok BEFORE MapStruct
- A multi-stage Dockerfile (build with Maven, run with eclipse-temurin:25-jre)
- A .dockerignore file
- A meaningful README.md with build and run instructions. Note: Docker is required on ALL profiles (no H2 fallback). README must cover: prerequisites, local dev setup (docker compose up postgres -d first), env vars for each profile, docker compose full-stack run, and a tests section noting Testcontainers requires Docker.