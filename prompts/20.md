Run `mvn clean verify` and make sure all tests pass. If any tests fail, fix them.
Then run `docker compose build` to verify the Docker build works.
Give me a summary of:
1. Total number of Java files created
2. Total number of test cases
3. Test coverage percentage from JaCoCo (if available)
4. List of all API endpoints with their HTTP methods