Create a Spring Boot 4.0.5 microservice called "order-service" with the following specifications:

- Group ID: org.example
- Artifact ID: order-service
- Layered package structure under org.example.orderservice:
    - controller
    - service
    - repository
    - model
    - dto
    - mapper
    - config
    - exception
    - event

Dependencies to include in pom.xml:
- spring-boot-starter-webmvc
- spring-boot-starter-data-jpa
- spring-boot-starter-validation
- spring-boot-starter-actuator
- spring-boot-starter-security
- springdoc-openapi-starter-webmvc-ui
- liquibase-core
- postgresql driver
- lombok
- mapstruct and mapstruct-processor
- spring-boot-starter-test
- testcontainers-junit-jupiter and testcontainers-postgresql
- spring-retry and spring-boot-starter-aspectj
- micrometer-registry-prometheus
- net.logstash.logback:logstash-logback-encoder for structured logging

Create application.yml with profiles:
- default/dev: H2 in-memory for quick local runs, logging level DEBUG
- staging: PostgreSQL connection using env vars (DB_HOST, DB_PORT, DB_NAME, DB_USER, DB_PASSWORD), connection pool of 10
- prod: same as staging but connection pool of 30, logging level WARN

Include:
- A multi-stage Dockerfile (build with Maven, run with eclipse-temurin:26-jre)
- A .dockerignore file
- A meaningful README.md with build and run instructions