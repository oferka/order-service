Create an ExternalInventoryClient in a new "client" package:

- Add @Retryable(maxAttempts = 3, backoff = @Backoff(delay = 1000, multiplier = 2)) on the method
- The method checkInventory(String productId, int quantity) should simulate a downstream HTTP call
- For the demo, have it randomly throw a RuntimeException 30% of the time to showcase retries
- Add a @Recover method that handles the fallback (log a warning, return a default response)
- Create a RetryConfig class with @EnableRetry

This shows Claude Code handles cross-cutting infrastructure patterns, not just CRUD.