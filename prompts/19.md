Create a k8s/ directory with production-ready Kubernetes manifests:

1. namespace.yml:
   - Create namespace "order-service"

2. configmap.yml:
   - Spring profile: staging
   - DB_HOST, DB_PORT, DB_NAME
   - JAVA_OPTS: -XX:MaxRAMPercentage=75.0 -XX:+UseG1GC

3. secret.yml (with placeholder base64 values and a comment to use sealed-secrets or external-secrets in production):
   - DB_USER, DB_PASSWORD, JWT_SECRET

4. deployment.yml:
   - 2 replicas
   - Container resource requests: 256Mi memory, 250m CPU
   - Container resource limits: 512Mi memory, 500m CPU
   - Readiness probe: /actuator/health (initial delay 30s, period 10s)
   - Liveness probe: /actuator/health (initial delay 60s, period 30s)
   - Startup probe: /actuator/health (failure threshold 30, period 10s)
   - Environment from ConfigMap and Secret
   - Image pull from ghcr.io
   - Pod anti-affinity: prefer spreading across nodes

5. service.yml:
   - ClusterIP service on port 80 → container port 8080

6. hpa.yml:
   - Min replicas: 2, Max replicas: 8
   - Target CPU utilization: 70%
   - Target memory utilization: 80%
   - Scale-down stabilization: 300s

7. ingress.yml (nginx ingress):
   - Host: order-service.acme.com
   - TLS with a placeholder secret name
   - Rate limiting annotations: 100 requests/second

Add a kustomization.yml that ties them all together.